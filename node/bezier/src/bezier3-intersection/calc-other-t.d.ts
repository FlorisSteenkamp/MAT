/**
 * Calculates the t-value of the closest point on Q to P(t).
 * @param δ
 * @param t
 * @param P
 * @param Q
 */
declare function calcOtherT(t: number, P: number[][], Q: number[][]): number;
export { calcOtherT };
