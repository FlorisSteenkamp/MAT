import { CpNode } from "../../cp-node/cp-node.js";
/**
 * @internal
 *
 * Use only for debugging. Check all nodes around the loop.
 */
declare function checkOrdering(cpNode: CpNode): void;
export { checkOrdering };
