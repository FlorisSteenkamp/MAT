import { CpNode } from '../../cp-node';
/**
 * Smoothens the given MAT by fitting consecutive mat lines with either lines or
 * quadratic or cubic beziers.
 */
declare function smoothen(cpNode: CpNode): void;
export { smoothen };
