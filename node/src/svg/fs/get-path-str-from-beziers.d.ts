import { Loop } from "../../loop";
/**
 * Returns a string representation of the given array of beziers.
 * @param loop
 * @param decimalPlaces
 */
declare function getPathStrFromBeziers(loop: Loop, decimalPlaces?: number): string;
export { getPathStrFromBeziers };
