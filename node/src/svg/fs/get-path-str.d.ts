import { Loop } from '../../loop';
/**
 * Returns a string representation of the given beziers linked loop.
 * @param beziers - A linked loop of cubic beziers.
 */
declare function getPathStr(loop: Loop, decimalPlaces?: number): string;
export { getPathStr };
