import { MatCircle } from '../../classes/mat-circle';
import { TTree } from './t-tree';
declare function addToTree(s: number, tree: TTree, coordinate: number, limits: number[][], node: MatCircle, key: string, depth: number): void;
export { addToTree };
