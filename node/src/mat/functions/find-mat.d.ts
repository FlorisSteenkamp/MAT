import { Loop } from '../../linked-list/loop';
import { Vertex } from '../classes/vertex/vertex';
/**
 * Find the MAT from the given Shape.
 * @param shape
 */
declare let findMat: (a: Loop[]) => Vertex;
export { findMat };
