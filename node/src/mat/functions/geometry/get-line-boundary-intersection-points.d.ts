import { ListNode } from '../../../linked-list/list-node';
import { ContactPoint } from '../../classes/contact-point';
/**
 * Get line shape intersection points.
 *
 * @param line A line described by two points
 * @param δ A boundary piece described by start and end contact points
 *
 * Currently not used
 */
declare function getLineBoundaryIntersectionPoints(line: number[][], δ: ListNode<ContactPoint>[]): number[][];
export { getLineBoundaryIntersectionPoints };
